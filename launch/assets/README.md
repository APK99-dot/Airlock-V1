# Launch image assets

Ready-to-post images for the LinkedIn sequence (see ../LINKEDIN.md).

| File | Use in | What it shows |
|------|--------|---------------|
| `post1-blocked-terminal.png` | **Post 1 (teaser)** | The agent's `DROP TABLE customers` getting BLOCKED, with an allowed refund above it for contrast |
| `post3-architecture.png` | **Post 3 (lesson)** | The allow / review / block flow — Airlock at the tool-call boundary + the ledger |

**Post 2 (launch)** uses the 40-second demo GIF (record it with ../DEMO_SCRIPT.md → save as ../../docs/demo.gif).

All images are 2x resolution (crisp on retina / when LinkedIn recompresses). Dark theme, brand-consistent.
Regenerate or tweak: the generators are simple Pillow scripts — ask and they can be re-run at any size.
